import React from "react";
import PropTypes from "prop-types";

const ListView = (props) => {
  return <div>this is the list view components</div>;
};

ListView.propTypes = {};

export default ListView;
